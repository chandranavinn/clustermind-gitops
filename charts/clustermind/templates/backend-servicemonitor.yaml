apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cm-backend
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: cm-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
