apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cm-frontend
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: cm-frontend
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
